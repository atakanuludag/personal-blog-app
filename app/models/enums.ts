export enum LayoutPageType {
  BlogPage,
  AdminPage,
  FullPage,
}

export enum OrderType {
  ASC = 'asc',
  DESC = 'desc',
}

export enum ArticleType {
  Post = 'post',
  Page = 'page',
}

export enum ValueType {
  Text = 'text',
  Multiline = 'multiline',
  Url = 'url',
  EMail = 'email',
  Number = 'number',
}
