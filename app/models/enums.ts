export enum LayoutPageType {
  BlogPage,
  AdminPage,
  FullPage,
}

export enum OrderType {
  ASC = -1,
  DESC = 0,
}

export enum ArticleType {
  Post = 'post',
  Page = 'page',
}

export enum PaletteMode {
  DARK = 'dark',
  LIGHT = 'light',
}
