export enum ArticleType {
  Post = 'post',
  Page = 'page',
}

export enum ValueType {
  Text = 'text',
  Multiline = 'multiline',
  Url = 'url',
  EMail = 'email',
  Number = 'number',
  PageSelects = 'pageSelects',
}
