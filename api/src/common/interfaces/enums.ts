export enum ValueType {
  Text = 'text',
  Multiline = 'multiline',
  Url = 'url',
  EMail = 'email',
  Number = 'number',
  PageSelects = 'pageSelects',
}

export enum OrderType {
  DESC = 1,
  ASC = -1,
}
